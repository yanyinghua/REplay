# 英语单词记忆小程序 · 设计方案

> 平台：微信小程序（原生）  ·  后端：微信云开发（云数据库 + 云函数 + 实时数据库）  ·  目标：用科学记忆法 + 游戏化，让背单词“记得住、玩得久、能社交”

---

## 0. 一句话定位

一款基于**间隔重复 + 主动回忆 + 双重编码（图像/动画）+ 趣味联想**的微信单词小程序。用户选词库闯关赚积分、打卡养习惯，并在好友/班级排行榜与实时 PK 中互相“卷动”学习。

---

## 1. 记忆科学 → 功能映射（核心设计依据）

所有玩法都不是为了“好看”，而是对应一条被验证的记忆原理。下表是设计的总纲：

| 记忆方法 | 原理 | 小程序中的落地玩法 |
|---|---|---|
| **艾宾浩斯遗忘曲线 + 间隔重复(SRS)** | 在即将遗忘前复习，记忆留存率最高 | 每个单词有「记忆强度」字段，复习间隔自动延长（如 5m→30m→12h→1d→3d→7d→15d）；到期单词自动进入「复习队列」 |
| **主动回忆(Active Recall)** | 努力从脑中提取，比重复看更有效 | 看义/图 → **遮盖拼写** → 默写或选词；答错才揭示答案 |
| **测试效应(Testing Effect)** | 测验本身增强记忆 | 每关以「闯关测验」收尾，而不是看完即过 |
| **双重编码(Dual Coding)** | 文字+图像同时编码，提取线索更多 | 每个词配**手绘风插画/动画**（见 §6），并鼓励用户自拍/画图联想 |
| **联想记忆法(Mnemonic)** | 把无意义音/形关联到熟悉事物 | **谐音梗卡片 + 故事联想 + 词根词缀拆解动画**（见 §6.2） |
| **组块化(Chunking)** | 把信息打包成组降低认知负荷 | 按「词族/主题/词根」成组学习，而非孤立单词 |
| **情境嵌入(Contextual)** | 在句子/场景中记词义更牢 | **例句填空 + 迷你情景漫画**，单词在语境里出现 |
| **多感官(多通道)** | 听+看+写+说多通道输入 | 真人/合成**发音**、**跟读打分**、手写描红 |
| **生成效应(Generation)** | 自己生成的内容记得更牢 | 用户**自编例句/联想**并存入个人词库 |
| **游戏化(Gamification)** | 即时反馈+目标感维持动机 | 积分、等级、徽章、关卡、排行榜、PK（见 §4、§5） |

---

## 2. 产品核心循环

```
选词库 → 学新词(动画/联想) → 主动回忆测验 → 得积分/通关 → 打卡
                                          ↓
                              复习队列(到期词) ──┐
                                          ↑______┘
      排行榜 / 好友PK ←── 社交驱动 ──→ 班级/房间一起学
```

每日循环：
1. **签到打卡**（streak 连续天数，断签衰减）
2. **新词学习**（当日计划量，如 20 词）
3. **复习提醒**（SRS 到期词）
4. **闯关测验**（完成当日任务领奖励）
5. **可选 PK / 排行榜冲刺**

---

## 3. 词库体系（学习者自选）

### 3.1 分级词库（按考试/学段）
- 小学 / 初中 / 高中 / 四级 / 六级 / 考研 / 雅思 / 托福 / GRE / 商务英语
- 每个词含：拼写、音标、发音、释义、例句、难度星级、词根词缀、常见搭配

### 3.2 主题词库（兴趣驱动，降低枯燥）
- 动物 / 食物 / 旅行 / 职场 / 电影台词 / 情绪 / 科技 / 节日 …

### 3.3 词根词缀库
- 按 `词根 + 词缀` 成组（如 `-spect 看`：inspect/respect/spectator），配合组块化学习

### 3.4 自定义词库
- 用户导入（文本/Excel 解析）或手动添加，支持拍照取词（OCR 待二期）

> 词库数据结构统一为 `words` 集合，便于复用 SRS 与所有玩法。详见 §8。

---

## 4. 积分 / 通关模式

### 4.1 成长数值
- **经验值 EXP**：学新词 +10，复习对 +5，闯关对 +15，连续打卡额外 +（天数×2）
- **等级 Level**：EXP 阈值升级（如 Lv1=0, Lv2=200…），解锁新玩法/词库/皮肤
- **金币 Coin**：用于抽卡（主题皮肤/插画）、复活（PK 用）、解锁隐藏词库

### 4.2 关卡设计
- 每个词库拆成**章节(Chapter) → 关卡(Level)**，每关 10~15 词
- 闯关条件：正确率 ≥ 阈值（如 80%）即通关，发星星（1~3 星按正确率）
- 通关奖励：EXP + 金币 + 可能掉「插画碎片」合成限定皮肤

### 4.3 成就 / 徽章
- 「七日不倒」「千词斩」「联想大师（自编联想≥50）」「PK 十连胜」「全词库满星」
- 徽章展示在个人主页，作为社交身份

### 4.4 打卡与连续天数
- 每日首学即打卡；连续天数 streak 计入排行榜「坚持榜」
- 断签后 streak 衰减（不复零，保留 50% 以减挫败感）

---

## 5. 多人 / 排名 / PK（云开发支撑）

### 5.1 排行榜（实时）
- **总榜**：按 EXP/等级
- **坚持榜**：按连续打卡天数
- **词库榜**：某词库通关进度/正确率
- **周榜/月榜**：周期性重置，保持活跃度
- 实现：云数据库聚合 + 实时监听（`db.collection().watch()`），榜单变化即时推送给前端

### 5.2 好友关系
- 基于微信关系链：`wx.getFriendCloudStorage` / 开放数据域展示好友排行榜
- 也可自建「关注/班级」体系（云数据库 `friends` / `classes` 集合）

### 5.3 PK 模式
两种形态，均用云开发实时数据库：

- **实时对战（1v1 房间）**：
  - 发起 → 匹配/邀请好友 → 进入房间（同一题池）→ 倒计时抢答 → 实时比分 → 结算（胜方 EXP/金币加成，负方安慰奖）
  - 断线/超时判负，防作弊（答案在云函数校验，不在前端）
- **异步挑战（车轮战）**：
  - 生成「挑战试卷」分享给好友，对方完成后对比得分，适合不同时在线的用户

### 5.4 班级 / 房间（多人同时学）
- 老师/家长建「班级」，成员可见班级排行榜与共同目标（如「本周共背 5000 词」）
- 班级内可发起团体 PK

---

## 6. 动画 / 图案 / 趣味联想详细设计（差异化重点）

### 6.1 双重编码插画
- 每个词配**统一画风的手绘动画插画**（如 `apple` → 会眨眼的红苹果掉落动画）
- 插画与词义强绑定，作为提取线索；用户可切换「显示/隐藏插画」做主动回忆
- 实现：Lottie / 帧动画 / CSS 动画，资源存云存储 `cloud://`

### 6.2 联想记忆卡（趣味核心）
- **谐音梗**：`ambulance 俺不能死 → 救护车` + 小漫画
- **象形/拆词**：`island → i(我)+s(蛇)+land(陆地)=我和小蛇在孤岛上`
- **词根动画**：词根像「积木」拼装成不同单词，直观展示构词
- 用户可**自编联想**并点赞热门联想，形成 UGC 联想库

### 6.3 记忆强化动画
- **翻转卡(Flip)**：正面词 → 点击翻转动画揭示义/图（主动回忆触发）
- **拼写描红**：手写轨迹动画 + 笔顺引导（多感官）
- **飞入/爆炸反馈**：答对词「飞进大脑」动画；连对触发 combo 特效
- **遗忘曲线可视化**：个人主页画一条「你的记忆留存曲线」，看到复习的价值

### 6.4 闯关与反馈动效
- 关卡进度条、星星弹出、金币飞舞、升级光效——即时正反馈维持多巴胺
- 错词「抖一抖 + 红色描边」并进入错词本

---

## 7. 技术架构（微信云开发）

```
微信小程序端 (WXML/WXSS/JS)
        │  wx.cloud
        ▼
┌──────────────────────────────────────┐
│  微信云开发                          │
│  ├─ 云数据库 (NoSQL, 实时 watch)     │
│  ├─ 云函数 (Node, 答题校验/PK/匹配)  │
│  ├─ 云存储 (插画/音频/词库包)        │
│  └─ 云调用 (微信登录/openid)         │
└──────────────────────────────────────┘
```

### 7.1 为什么选云开发
- 免运维、天然微信登录（`cloud.getWXContext().OPENID`）
- 实时数据库支撑排行榜/PK 房间
- 弹性扩容，适合学生寒暑假流量波动

### 7.2 关键云函数
- `login`：静默登录拿 openid，建/取用户档案
- `gradeAnswer`：校验答题正确性（防前端作弊，PK 必备）
- `matchPK`：匹配对手 / 创建房间
- `syncProgress`：同步学习进度与积分
- `importWordbook`：解析自定义词库

---

## 8. 数据模型（云数据库集合）

```js
// users 用户档案
{ _openid, nickName, avatar, exp, level, coin,
  streak, lastCheckin, badges:[], createdAt }

// words 词库（公共，按 bookId 分组）
{ wordId, bookId, spell, phonetic, audioUrl,
  meaning, example, root, tags:[], difficulty, illustration }

// user_words 个人学习状态（SRS 核心）
{ _openid, wordId, bookId,
  ease,        // 记忆难度因子
  interval,    // 当前间隔(天)
  due,         // 下次复习时间
  reps,        // 复习次数
  lapses,      // 遗忘次数
  lastReview }

// progress 关卡进度
{ _openid, bookId, chapter, level, stars, passed, bestAccuracy }

// pk_rooms PK 房间
{ roomId, players:[{_openid,score,ready}],
  questionPool:[], status, createdAt }

// leaderboard / classes / friends 排行榜快照 / 班级 / 好友
```

> SRS 采用经典 **SM-2 算法**（Anki 同款）简化版，落地在 `user_words`。

---

## 9. 原型页面结构（信息架构）

| 页面 | 路径 | 关键内容 |
|---|---|---|
| 启动/登录 | `pages/launch` | 微信授权、初始化档案 |
| 首页/今日 | `pages/home` | 今日任务、打卡、记忆曲线、快捷入口 |
| 词库选择 | `pages/books` | 分级/主题/自定义词库网格 |
| 学习页 | `pages/study` | 翻转卡 + 插画 + 联想卡 + 发音 |
| 闯关测验 | `pages/quiz` | 主动回忆题、拼写、选义、计时 |
| 复习页 | `pages/review` | SRS 到期词队列 |
| 关卡地图 | `pages/map` | 章节/关卡进度、星星 |
| 我的 | `pages/mine` | 等级/徽章/连续天数/自编联想 |
| 排行榜 | `pages/rank` | 总榜/坚持榜/词库榜（实时） |
| PK 大厅 | `pages/pk` | 匹配/邀请/房间/实时对战 |
| 班级 | `pages/class` | 班级榜/团体目标 |
| 错词本 | `pages/mistakes` | 易错词强化 |

**主流程**：`launch → home → books → study → quiz → (map星星) → home`
**社交流**：`home → rank / pk / class`

---

## 10. 实施路线图（分阶段，确认后按此写代码）

- **M1 单人闭环（先跑通记忆玩法）**
  云开发初始化 + 登录 + 词库 + 学习页(翻转卡/插画/联想) + SRS复习 + 闯关测验 + 我的页
- **M2 成长系统**
  积分/等级/金币/徽章/打卡/记忆曲线可视化 + 错词本
- **M3 社交与竞争**
  好友榜 + 总榜/坚持榜实时 + PK 实时对战 + 异步挑战 + 班级
- **M4 内容与运营**
  更多词库、UGC 联想、皮肤/插画抽取、签到活动

---

## 11. 待你确认的关键决策

1. **词库来源**：初期用内置示例词库（几百词演示），还是接入现成词库数据（如开源 CET 词表）？
2. **插画资源**：自绘 SVG/Lottie（可控但耗时）vs 用 emoji/图标占位先做原型？
3. **PK 实时性**：先做「异步挑战」降低复杂度，还是直接上实时房间？
4. **范围**：本次是否只交付文档，还是文档确认后直接进入 M1 写可运行原型？

---

> 下一步：你确认上述决策（尤其 §11）后，我将按 M1 搭建微信云开发项目骨架并实现核心记忆玩法页面（学习页 + 复习 + 闯关）。
